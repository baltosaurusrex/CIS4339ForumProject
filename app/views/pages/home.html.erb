<html>
<div class="list">

  <% Channel.all.each do |x| %>
      <div class="channelname">
        <h2 class="title"><%= link_to h(x.name), channel_path(x.id)%></h2>
        <p class="description"><%= x.description%></p>
      </div>
  <%end%>

</div>
</html>
