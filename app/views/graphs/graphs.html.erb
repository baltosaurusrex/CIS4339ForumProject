<%= line_chart Post.group_by_day(:created_at).count, colors:['#787669' ] %>

<%= pie_chart Post.joins(:channel).group(:name).count%>

<%-# line_chart Post.joins(:channel).group(:name).group_by_day(:created_at).count%>
